services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sub
      POSTGRES_PASSWORD: sub
      POSTGRES_DB: sub
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
